{
  "name": "trie-connect",
  "private": true,
  "version": "2.0.0",
  "workspaces": ["frontend", "server"],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev --workspace server\"",
    "build": "npm run build:cpp && npm run build --workspace frontend",
    "build:cpp": "cmake -S cpp -B cpp/build && cmake --build cpp/build --config Release",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
